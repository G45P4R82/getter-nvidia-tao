[pytest]
testpaths = tests
markers =
    auth: tests that require a valid NGC key or TAO JWT
    integration: tests that create and delete TAO resources
    frontier: malformed, invalid, and boundary input tests
addopts = -ra
